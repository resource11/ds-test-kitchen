import { Markdown, Meta } from '@storybook/addon-docs/blocks';
import layout from '../docs/layout.md?raw';

<Meta title="Layout" />

{/* The page shows docs/layout.md itself, so there is one text to keep current. */}

<Markdown>{layout}</Markdown>
