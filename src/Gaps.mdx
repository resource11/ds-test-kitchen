import { Markdown, Meta } from '@storybook/addon-docs/blocks';
import gaps from '../figma/GAPS.md?raw';

<Meta title="Gaps" />

{/* The page shows figma/GAPS.md itself: where Figma cannot match the code, and why, plus what is open. */}

<Markdown>{gaps}</Markdown>
