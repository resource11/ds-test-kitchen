import { Markdown, Meta } from '@storybook/addon-docs/blocks';
import guide from '../docs/figma-only.md?raw';

<Meta title="Figma only" />

{/* The page shows docs/figma-only.md itself, so there is one text to keep current. */}

<Markdown>{guide}</Markdown>
