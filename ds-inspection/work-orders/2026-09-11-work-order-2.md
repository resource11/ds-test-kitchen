# Work Order — Sample Design System (`sds`), second pass

_From inspection: `reports/2026-09-11-inspection-2.md` · Written: 2026-09-11 (afternoon) · **Replaces** `2026-09-11-work-order.md`_

This morning's work order still stands: 0 of its 14 items are closed, because `main` hasn't moved. This version keeps every item and adds what the second pass found. Reds get fixed now, yellows get scheduled, greens get left alone. Every item cites its station and evidence. The team owns prioritisation, which here means Christine; this is the technician's recommendation.

**Who:** 🎨 Design · 🛠️ Dev · 🤝 Shared (both have a part)

## At a glance

|  # | Light | Who | Fix                                                                  | Effort | New? |
|---:|:-----:|:---:|:---------------------------------------------------------------------|:------:|:----:|
|  1 |  🔴   | 🤝  | Retune the colours so every pair passes in both modes; CI checks it   | M      |      |
|  2 |  🔴   | 🛠️  | Render every story in CI, with an accessibility check                | M      |      |
|  3 |  🔴   | 🛠️  | Fix the Checkbox crash (and SignUpForm)                              | S      | ◐    |
|  4 |  🟡   | 🛠️  | Run `validate` after the Storybook build                             | S      | ◐    |
|  5 |  🟡   | 🛠️  | Protect `main`                                                       | S      |      |
|  6 |  🟡   | 🤝  | Make the Figma check two-way, and notice changed values              | M      |      |
|  7 |  🟡   | 🤝  | "Mirrored in Figma" is part of done; bring `design` up to date       | S      |      |
|  8 |  🟡   | 🛠️  | First release: template, tag, changelog, owner, issues               | S      |      |
|  9 |  🟡   | 🛠️  | Fix the 19 stale docs lines                                          | S      | ◐    |
| 10 |  🟡   | 🤝  | One word per idea: Toast `error` → `danger`; write the words down    | S–M    | ◐    |
| 11 |  🟡   | 🤝  | "When to use" notes, compound parts, colour roles                    | M      |      |
| 12 |  🟡   | 🎨  | Clean the HTML codes out of 37 Figma descriptions                    | S      | ◐    |
| 13 |  🟡   | 🛠️  | Make the patterns follow the rules, and reach agents                 | M      | ◐    |
| 14 |  🟡   | 🤝  | Connect your agent: `.mcp.json`, `AGENTS.md`, a guide, publish       | S      |      |
| 15 |  🟡   | 🛠️  | Enforce the pixel, right-to-left and reduced-motion rules            | M      |      |
| 16 |  🟡   | 🎨  | Decide the open design questions                                     | S each |      |
| 17 |  🟡   | 🤝  | Error, focus and keyboard specs for designers                        | M      | ◐    |
| 18 |  🟡   | 🤝  | Visual regression (Chromatic)                                        | M      | ✚    |
| 19 |  🟡   | 🤝  | A second workflow: prototype a screen from the system                | M      | ✚    |
| 20 |  🟡   | 🤝  | Give `Select.Separator` a look, in code and in Figma                 | S      |      |
| 21 |  🟡   | 🎨  | Pick which missing staples the course needs                          | M–L    | ✚    |

**New?** ✚ new this pass · ◐ same item, bigger or sharper than this morning.

**The designer's list, in order:**
1. #1 pick the colours
2. #16 decide the open questions
3. #10 agree the words
4. #17 design the error and focus states
5. #11 write the notes
6. #20 mirror the separator once it has a look
7. #7 own the Figma step, and commit or park the `design` work
8. #12 check the cleaned descriptions
9. #21 pick the missing staples
10. #14 try the connect guide
11. #18 review the visual diffs
12. #19 define the prototype flow

## 🔴 Fix now (reds)

### 1. 🤝 Retune the colours so every pair passes, in light and dark, and make CI check it
- **Station:** 3, Accessibility · **Evidence:** [verified] 28 of 80 painted colour pairs fail, 8 in light and 20 in dark. Figma's variables match the code value for value, so Figma fails the same 28. Axe confirms it in 6 rendered stories.
  - **Dark:** primary Button text 4.47:1 (pressed 1.99:1); danger Button 3.76:1; Alert body text 2.42–3.12:1; Badge text 1.72–2.56:1; field error text 4.10:1; all four Alert icons under 3:1.
  - **Light:** Badge status text 2.86–3.95:1.
  - **Both:** input, select and textarea borders 1.30:1 (light) and 1.89:1 (dark); Checkbox box and Switch off-track 1.42–1.49:1 (light).
- **🎨 Design part:**
  - Choose the new values, starting from the "Colour decisions" board (1A/1B, 2A/2B; recommended 1B + 2A).
  - Add one decision that board doesn't cover yet: a control-border colour of at least 3:1, possibly a new `border.control`.
  - The agent can propose passing options; the look is your call.
- **🛠️ Dev part:**
  1. Extend `ds-inspection/checks/contrast-pairs.mjs` to the 40 painted pairs. Inspector B's list is in the scratchpad as `contrast-extended.mjs`.
  2. Move the script into `scripts/` and call it from `validate --strict`.
  3. Put the chosen values into `tokens/tier-2-usage/semantic.*.json`, then run the figma-mirror "tokens changed" procedure.
  4. Correct the Foundations page line saying the `on-*` colours are "contrast-paired".
- **Why it's first:** one fix at the token level lands in every component and in Figma.
- **Done when:** `validate --strict` fails on today's tokens and passes on the new ones, 0 pairs fail in both modes on both sides, and CI is green.
- **Effort:** M

### 2. 🛠️ Render every story in CI, with an accessibility check
- **Station:** 5 (also 3) · **Evidence:** [verified] 0 tests, 0 `play` functions and no test runner; 251 stories are compiled but never opened. `a11y: { test: 'error' }` has nothing behind it. The Checkbox and SignUpForm crashes merged with CI green.
- **First move:**
  - Add the Storybook Vitest addon or the test runner, and fail on render errors and axe violations in light and dark.
  - Mark Switch's `DescriptionOnly` teaching story as an expected failure.
  - Then add keyboard `play` tests for Tabs, Menu, Select and Dialog: open, arrows, Escape, focus return. The harness saw Dialog's focus land on `<body>` after Escape; this is where to confirm it.
- **Note:** it's a new dev dependency, which bumps against `CLAUDE.md` rule 5 in spirit. Your call.
- **Done when:** undoing the Checkbox fix turns CI red.
- **Effort:** M

### 3. 🛠️ Fix the standalone Checkbox (and SignUpForm)
- **Station:** 1, 3, 5 · **Evidence:** [verified] `Checkbox.tsx` uses `Field.Item` with no `Field.Root`, and Base UI throws "FieldRootContext is missing". All 8 of 8 Checkbox stories crash, and (new, confirmed) both SignUpForm stories too.
- **First move:** wrap the standalone Checkbox in `Field.Root` when it isn't inside a group, following Switch's pattern. Keep the `Field.Label` and `Field.Description` part names, which the Figma contract uses.
- **🎨 Heads-up:** the Checkbox stories are blank today. After the fix, remove `GAPS.md` row 27 and check the Figma Checkbox against the story.
- **Done when:** every Checkbox, CheckboxGroup and SignUpForm story renders, with a clean axe panel.
- **Effort:** S

## 🟡 Schedule (yellows)

### 4. 🛠️ Run `validate` after the Storybook build
- **Station:** 5, 6, 9 · **Evidence:** [verified] `ci.yml:22-24` runs `validate --strict` before `build-storybook`, and the CI log says "components.json not found". So the manifest-gap check and the Figma variant-value and default checks never run on a PR: mutations M6 and M7 fail locally but pass CI.
- **First move:** swap the two steps.
- **Done when:** the CI log no longer shows the note, and M6/M7 fail CI.
- **Effort:** S · **Timing:** now (one line)

### 5. 🛠️ Protect `main`
- **Station:** 7, 5 · **Evidence:** [verified] protection 404, rulesets `[]`; PR #1 merged with no checks.
- **First move:** require a PR and the **`build`** check, and block force-pushes. Correct `branching.md`, which calls the check `ci`.
- **Done when:** the protection endpoint returns 200.
- **Effort:** S · **Timing:** now (five minutes)

### 6. 🤝 Make the Figma check two-way, and notice changed values
- **Station:** 6, 5 · **Evidence:** [verified] of 11 mutations, only 3 fail CI.
  - These pass everything: a component deleted from the manifest (M1), a new variant value in code (M2), a changed token value (M3), a changed stylesheet (M4), a new component (M9) and a deleted `GAPS.md` row (M10).
  - The check compares against the committed snapshot, so a Figma edit is invisible until someone re-snapshots.
- **🎨 Why designers care:** this is what will tell you when a Figma component no longer matches the code. Until it lands, nothing will.
- **🎨 Design part:**
  - List Form, ScrollArea and ContextMenu, each with its reason, in `figma/GAPS.md`.
  - Re-snapshot after every Figma edit.
- **🛠️ Dev part:**
  1. Add a rule: a folder in `src/components/` with no manifest `source` and no `GAPS.md` row fails.
  2. Check every code variant value is present in Figma.
  3. Store resolved token values and a hash of each component's CSS in `figma/manifest.json`, so a changed stylesheet reports "Figma may be out of date".
- **Done when:** M1–M4, M9 and M10 each produce a finding.
- **Effort:** M · **Timing:** this sprint

### 7. 🤝 Make "mirrored in Figma" part of done, and bring `design` up to date
- **Station:** 6, 10 · **Evidence:** [verified] the Figma step is missing from `conventions.md`, CONTRIBUTING and `CLAUDE.md`'s "after you change UI" list, and there's no PR template. `design` is 15 commits behind `main`, 0 ahead.
- **🎨 Design part:**
  - Own the step "manifest re-snapshotted, screenshot compared, or a `GAPS.md` row added".
  - Commit or park the booking and blog prototype work that's uncommitted on `design`.
  - Enable the published library in your prototype files.
- **🛠️ Dev part:**
  1. Add the step to `conventions.md` and a PR template.
  2. Merge `main` into `design`.
- **Done when:** the compare shows 0 behind, and the next PR uses the checklist.
- **Effort:** S · **Timing:** this week

### 8. 🛠️ First release: template, tag, changelog, owner, issues
- **Station:** 7, 1 · **Evidence:** [verified] 0 tags, 0 releases and no CHANGELOG ever. `is_template: false`, although `branching.md:38` says "Use this template". No named owner and no CODEOWNERS. 0 issues: the known bugs live in `GAPS.md`, the PR #2 body and the brief.
- **First move:**
  1. Tick "Template repository".
  2. File GitHub issues for Checkbox, `Select.Separator` and ToggleGroup, and add bug and PR templates.
  3. After PR #3 and item 3, tag `v0.1.0` and generate `CHANGELOG.md` from the Conventional Commits.
  4. Add a "Maintainer:" line and CODEOWNERS.
  5. Turn on delete-branch-on-merge.
- **Done when:** "Use this template" exists and `gh release list` shows `v0.1.0`.
- **Effort:** S · **Timing:** this sprint

### 9. 🛠️ Fix the 19 stale docs lines
- **Station:** 6, 4, 7 · **Evidence:** [verified] the full list is in Station 6 of the report. It includes "Gitflow", Code Connect twice, "nothing catches that drift", the IconButton claims in `CLAUDE.md:47` and `GettingStarted.mdx:37`, "5 properties", the brief's "in review" and "next: publish", `CLAUDE.md` over-claiming what CI checks, and the brief's "addon-mcp ships one tool" (it serves 7). Also 4 story titles have no group.
- **First move:** merge PR #3 (clears 5), then one `docs:` commit for the rest. Add one line to `CLAUDE.md` saying which wins when the Storybook MCP and `CLAUDE.md` disagree on where props come from.
- **🎨 Heads-up:** grouping the 4 stories moves Button, Dialog, Switch and TextField in the Storybook sidebar.
- **Done when:** a grep for Gitflow, Code Connect and "Nothing catches" returns nothing.
- **Effort:** S · **Timing:** this sprint

### 10. 🤝 One word per idea: Toast `error` → `danger`, and write the words down
- **Station:** 4 · **Evidence:** [verified] everything says `danger` except Toast (`type=error`, in code and Figma). Toast `success` draws the accent border, not the success colour. Button says `primary` for what Badge and Meter call `accent`. `clearable` sits beside `show*`. The vocabulary is written only in the brief.
- **🎨 Design part:**
  - Agree the words: `danger`; `primary` (action hierarchy) vs `accent` (emphasis); `show*` for optional parts.
  - Decide Toast success → success colour, or keep accent with a note.
- **🛠️ Dev part:**
  1. Add a prop-vocabulary section to `conventions.md`.
  2. Rename Toast's value in code and stories, then mirror it in Figma.
  3. Optionally, a `validate` warning for off-list values.
- **Done when:** no `error` value is left in the props, and Figma matches.
- **Effort:** S–M · **Timing:** before students start copying names

### 11. 🤝 "When to use" notes, compound parts and colour roles, where people and AI read them
- **Station:** 9, 1 · **Evidence:** [verified]
  - 14 of 42 components have no description, and only 4 of 42 say when to use them over a neighbour.
  - `RadioGroupItem`, `FormActions` and Card's parts have no manifest entries, and Tabs, Menubar, NavigationMenu, Toolbar and Toast document 0 props. The test agent learned Toast only from its story.
  - 2 of 35 colour roles have a description.
- **🎨 Design part:**
  - Write the rules for the pairs agents and people confuse: Tooltip / Popover / PreviewCard, Dialog / AlertDialog, Select / Combobox / Autocomplete, Toggle / Switch / Checkbox, Menu / ContextMenu / Menubar.
  - Settle Switch vs Checkbox on a form with Save: the SettingsPage pattern and Switch's own description disagree.
  - Write one line per colour role.
- **🛠️ Dev part:** JSDoc on each exported component and part, so the manifest and the Storybook MCP carry the notes; `$description` on the colour tokens.
- **Done when:** 42 of 42 components and 35 of 35 colour roles have a note, and `docs-show` lists the part props.
- **Effort:** M · **Timing:** this quarter

### 12. 🎨 Clean the HTML codes out of 37 Figma descriptions
- **Station:** 9, 2 · **Evidence:** [verified] 37 of 97 descriptions show 76 raw codes in all, e.g. `Rendered as &lt;span aria-current=&quot;page&quot;&gt;`. The orchestrator spot-checked the Breadcrumb page: 3 of 4 affected.
- **🎨 Design part:** check the result in the library.
- **🛠️ Dev part:** write plain text from the generator and re-set only those 37 in one pass. Keep the "Contract — src/…" line, which `snapshot.figma.js` reads.
- **Done when:** a sweep finds 0 codes, and the snapshot still resolves every source path.
- **Effort:** S · **Timing:** this sprint

### 13. 🛠️ Make the patterns follow the rules, and reach agents
- **Station:** 9, 10 · **Evidence:** [verified] `src/patterns/Patterns.module.css` sets font sizes and line heights by hand, and `validate`'s text-style rules only scan `src/components/`. An agent that copies the patterns goes off-system while every check passes. Through the Storybook MCP, the 4 patterns return story names but no code.
- **First move:**
  1. Extend the type rules to `src/patterns/` and fix the patterns.
  2. Give each pattern a resolvable docs entry.
  3. Remove the duplicate relative import in the RadioGroup story snippets and the inline width in the Tabs stories.
- **Done when:** `validate` flags a hand-set font size in a pattern, and `docs-show patterns-*` returns code.
- **Effort:** M · **Timing:** this sprint

### 14. 🤝 Connect your agent: `.mcp.json`, `AGENTS.md`, a guide, publish
- **Station:** 10 · **Evidence:** [verified] the Storybook MCP serves 7 tools on `main`, but no config registers it anywhere. There's no `AGENTS.md`, and no doc says how to connect an agent to Storybook or Figma. Port 6006 serves the `design` folder. No published Storybook yet (PR #4 open).
- **🎨 Design part:** plain steps for designers: Figma MCP, the Storybook link, what to ask.
- **🛠️ Dev part:**
  1. Commit `.mcp.json` registering `http://localhost:6006/mcp`, noting it needs `npm run storybook`.
  2. Add `AGENTS.md` pointing to the rules.
  3. Merge PR #4.
- **Done when:** a fresh clone in Claude Code lists the 7 tools, and a student can wire both bridges from the docs alone.
- **Effort:** S · **Timing:** this sprint

### 15. 🛠️ Enforce the pixel, right-to-left and reduced-motion rules
- **Station:** 2, 3 · **Evidence:** [verified] only 5 component stylesheets handle reduced motion (Dialog, Menu, Select, Switch and the Tabs indicator always animate), and there's no global fallback.
  - 19 left/right declarations in 12 files would break right-to-left layouts: Menu's shortcut, Table's `text-align`, Toast, the popup arrows, NavigationMenu and the Tabs indicator. There are only 24 logical declarations in all.
  - Control heights (40/32/48px, 16 times), 2px nudges and `-13px` arrow offsets are raw, and `validate` has no pixel or physical-property rule.
  - `--sds-font-sans`, a tier-1 token, sits in 17 component files, but `CLAUDE.md`'s allowed list doesn't name it.
- **First move:**
  1. Add `validate` rules for raw pixels and physical properties, then convert the flagged declarations.
  2. In `base.css`, set the `--sds-duration-*` tokens near 0 inside `@media (prefers-reduced-motion: reduce)`.
  3. Add `--sds-font-sans` to `CLAUDE.md`'s allowed list, or stop using it.
- **🎨 Heads-up:** whether control heights get a size token is a design decision (see #16).
- **Effort:** M · **Timing:** this quarter

### 16. 🎨 Decide the open design questions
- **Station:** 2, 4 · **Evidence:** [verified]
  - Alert's warning and danger icons are the reverse of the usual convention, in code and Figma.
  - Menu uses a line height no text style has (10 Figma layers carry a raw 120%).
  - ToggleGroup, not Toggle, applies the segmented look.
  - **Icon drift is slightly bigger than logged:** 4 chevron drawings (two down, two right) and 2 check marks with different strokes (1.75 vs 2).
  - `primary` vs `accent` (see #10).
  - A size scale for control heights. `min-height` of 40/32/48px is written raw 16 times, in code and in Figma.
- **First move:** decide each one and record it in `docs/decisions.md` (PR #3). The code change follows, and Figma follows the code.
- **Effort:** S per decision · **Timing:** before students start copying names

### 17. 🤝 Error, focus and keyboard specs for designers
- **Station:** 1, 3 · **Evidence:** [verified]
  - TextField, Select and Checkbox have no error or focus example in Figma.
  - Code has no per-component `Invalid` story either: errors only appear in the `Form` and `SignUpForm` stories. So a designer can't draw a form with a failed field.
  - 0 annotations on the 11 pages read, and no annotation kit (only Figma's 4 default categories).
  - The missing variants are a documented decision (`GAPS.md` lines 28, 36, 54, 60: CSS states, not props).
- **🎨 Design part:**
  - Decide how focus and error look, and check them against contrast (#1).
  - Add a "States" frame (invalid, focus) to the TextField, Textarea, Select, Checkbox and RadioGroup pages. Use examples, not variants, which respects `GAPS.md`.
  - Add Accessibility annotations for keyboard and focus order on Dialog, Menu, Tabs and Select.
- **🛠️ Dev part:** an `Invalid` story for each form control, so the Figma frame has a code reference to match.
- **Done when:** each of the 5 form pages shows an invalid state that matches its story, and every overlay carries a keyboard note.
- **Effort:** M · **Timing:** this quarter

### 18. 🤝 Visual regression (Chromatic)
- **Station:** 5 · **Evidence:** [verified] nothing watches for visual breakage; [reported] Chromatic is blocked on a token.
- **🎨 Design part:** you review the visual diffs on each PR; that's the designer's seat in CI.
- **🛠️ Dev part:** add the project token as a repo secret and a Chromatic step, before the colour retune (#1) lands, so the retune shows up as a reviewable diff.
- **Done when:** a token change shows a visual diff on its PR.
- **Effort:** M · **Timing:** before #1 if possible

### 19. 🤝 A second workflow: prototype a screen from the system
- **Station:** 10 · **Evidence:** [verified] the only skill that ships with the repo is `figma-mirror` (code → Figma). Nothing covers the way back: a Figma frame or brief turned into a screen on `design`.
- **🎨 Design part:** define the flow from today's Version C booking prototype: what you hand over, what you get back, where it lives.
- **🛠️ Dev part:** write it as a project skill and link it from `CLAUDE.md`.
- **Done when:** the skill is in `.claude/skills/` and was used for one real screen.
- **Effort:** M · **Timing:** this quarter

### 20. 🤝 Give `Select.Separator` a look, in code and in Figma
- **Station:** 1 · **Evidence:** [verified] `Select.tsx:220` exports `BaseSelect.Separator` with no class, and `Select.module.css` has no rule for it, so it renders nothing. There's no Figma component for it.
- **🎨 Design part:** once it's styled, mirror it in the library (it will reuse the `Separator` look).
- **🛠️ Dev part:** add a `.separator` rule using the border token, plus a story with grouped items. File it as an issue first (#8).
- **Done when:** the separator is visible in the story and exists in Figma.
- **Effort:** S · **Timing:** this sprint

### 21. 🎨 Pick which missing staples the course needs
- **Station:** 1, 2 · **Evidence:** [verified]
  - There's no Link, Pagination or empty-state pattern, and no Button `loading` state.
  - Against Material, Carbon and Primer, the system has no date picker, stepper, tree view, skeleton, file upload or search field. That benchmark is from the inspectors' knowledge; no knowledge MCP was connected.
  - Component pages have no anatomy or do/don't, and there's no accessibility or right-to-left guidance page.
- **First move:** choose the few that the course's example screens actually need; Link and an empty state are the likely first two. Not every gap deserves a component. Each one you pick goes code first, then Figma.
- **Effort:** M–L · **Timing:** before the course material is fixed

## 🔧 Access upgrades (sharper next inspection)

- **Use the Figma Console bridge for whole-file sweeps.** It's connected (port 9223, with this file open), and `figma_audit_design_system` can score the whole library in one call. Close the stale second instance on port 9224 (pid 13451), and take the plugin update it offers.
- **Connect a design-systems knowledge MCP,** so the coverage benchmark can be cited, not recalled.
- **Give each inspector its own build cache.** The inspectors linked the real repo's `node_modules`, so `tsc -b` wrote its cache into `node_modules/.tmp` there. The cache is gitignored and harmless, but it isn't clean. Next time, point `tsBuildInfoFile` at the scratchpad or copy `node_modules`.
- **Pin Storybook for inspections to `main`.** Port 6006 serves the `design` folder; this pass started private servers from a `main` export instead. A second `launch.json` entry would make that one click.

## 🟢 Keeping the greens green

- **Station 1 (coverage):** after every component change, run the `figma-mirror` skill and republish the library, so the three legs stay even. Item 6 turns "every component mirrored or listed in `GAPS.md`" into a check.
- **Stations 4 and 9 were green this morning, and are one point short now:**
  - Keep the name checks in `validate --strict`.
  - Re-run the generation test after big changes, and track its cost: 46 calls and about 50k tokens this pass.

## Cadence

- **Re-inspect (deep, all stations):** 2026-12-11. No full re-run on an unchanged commit: this pass shows it adds finer evidence but ±1 of noise per station.
- **Quick passes:**
  - Stations 3 and 5, as soon as items 1–3 land.
  - Station 6, after items 4 and 6.
  - Stations 9 and 10, after items 11, 13 and 14, re-running the generation test.
- **Wire into CI now:** the contrast check (#1), a story render with axe (#2), and `validate` after the Storybook build (#4).
- **Owner:** Christine Vallaure. **Review:** at the start of each work session, until items 1–5 are closed.
